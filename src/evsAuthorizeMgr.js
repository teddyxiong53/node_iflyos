const { EventEmitter } = require("events");
const console = require('console')
const log = console.log
const EventEmitter = require('events')

class EvsAuthrizeMgr extends EventEmitter {
    constructor() {
        super()
    }

}

module.exports = EvsAuthrizeMgr
